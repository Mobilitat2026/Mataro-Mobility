# Mataró Mobility
Ejecución

Esta aplicación debe ejecutarse desde un servidor HTTP (por ejemplo, Visual Studio Code + Live Server). No funcionará correctamente abriendo directamente el archivo index.html debido a las restricciones de seguridad del navegador (CORS).